import { defineConfig } from 'astro/config';

export default defineConfig({
  // Cuando tengas dominio, reemplazá por: https://ladivergentedigital.com.ar
  site: "https://TU-DOMINIO-AQUI",
  output: "static",
});
